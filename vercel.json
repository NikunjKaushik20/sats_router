{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx tsx scripts/seed.ts && next build",
  "installCommand": "npm install",
  "env": {
    "DATABASE_URL": "file:./dev.db"
  }
}
